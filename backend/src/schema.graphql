# import * from './generated/prisma.graphql'

type Mutation {
   createItem(title: String!, decription: String!, image: String, largeImage: String, price: Int!): Item!
}

type Query {
    items: [Item]!
}
